# 🚀 Быстрый старт: Анализ таблиц через нейросети

> **Новая функция:** Отправляйте первые 15 строк вашей таблицы в GigaChat и Proxy API для получения аналитического анализа!

---

## ⚡ 30-секундный старт

### 1️⃣ Загрузите файл (в браузере)
```
1. Откройте http://localhost:3000
2. Нажмите "Загрузить файл"
3. Выберите CSV, Excel или PDF
```

### 2️⃣ Анализируйте первые 15 строк
```javascript
// В браузерной консоли (F12)
fetch('/api/table-analysis', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({})
})
.then(r => r.json())
.then(data => console.log(data))
```

### 3️⃣ Получите результаты
```json
{
  "status": "success",
  "giga_result": "Анализ GigaChat: В данных видны...",
  "proxy_result": "Анализ Proxy API: Наблюдается...",
  "errors": {}
}
```

---

## 📋 Что это дает?

| Функция | Описание | Время |
|---------|---------|-------|
| 📊 AI Анализ | Автоматический анализ первых строк таблицы | < 10 сек |
| 🔍 Две моделью | Результаты от GigaChat и Proxy API | Параллельно |
| ⚙️ Системный промпт | Профессиональная аналитика без настроек | Встроенный |
| 🛡️ Обработка ошибок | Graceful degradation если API недоступен | Автоматично |

---

## 🔧 Установка (если нужна)

### Предварительные требования
- Python 3.8+
- Виртуальное окружение
- GigaChat token и Proxy API key

### Шаги установки
```bash
# Если еще не установлено
cd d:\Курсор\DataAnalitics_AI-Flask

# Активируем виртуальное окружение
.\.venv\Scripts\Activate.ps1

# Устанавливаем зависимости (если нужно)
pip install -r requirements.txt

# Запускаем приложение
python run.py
```

---

## 📖 Основные команды

### Запуск сервера
```bash
python run.py
```
Сервер откроется на `http://localhost:3000`

### Тестирование (Unit)
```bash
.\.venv\Scripts\python.exe test_table_analysis.py
```

### Тестирование (Integration)
```bash
.\.venv\Scripts\python.exe test_table_analysis_integration.py
```

### Примеры API
```bash
.\test_table_analysis_api.ps1
```

---

## 🎯 Основные эндпоинты

### 1. Загрузить файл
```bash
POST /api/upload
Content-Type: multipart/form-data

[файл CSV/Excel/PDF]
```

### 2. Анализировать таблицу ⭐ НОВЫЙ
```bash
POST /api/table-analysis
Content-Type: application/json

{
  "rows_count": 15
}
```

### 3. Получить данные
```bash
GET /api/data?offset=0&limit=15
```

---

## 💡 Примеры использования

### Пример 1: Анализировать все загруженные данные
```html
<button onclick="analyzeAll()">Анализировать всю таблицу</button>

<script>
async function analyzeAll() {
  const response = await fetch('/api/table-analysis', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ rows_count: 999 })
  });
  const data = await response.json();
  console.log('Результаты:', data);
}
</script>
```

### Пример 2: Сравнение 5, 10 и 15 строк
```python
import requests

for rows in [5, 10, 15]:
    response = requests.post(
        'http://localhost:3000/api/table-analysis',
        json={'rows_count': rows}
    )
    print(f"\n=== Анализ {rows} строк ===")
    print(response.json())
```

### Пример 3: Интеграция в UI
```html
<div class="analysis-section">
  <h2>AI Анализ таблицы</h2>
  
  <div class="controls">
    <input type="number" id="rows" value="15" placeholder="Количество строк">
    <button onclick="startAnalysis()">Анализировать</button>
  </div>
  
  <div id="results" style="display: none;">
    <div class="result-block">
      <h3>🤖 GigaChat</h3>
      <p id="giga-text"></p>
    </div>
    <div class="result-block">
      <h3>🤖 Proxy API</h3>
      <p id="proxy-text"></p>
    </div>
  </div>
</div>

<script>
async function startAnalysis() {
  const rows = document.getElementById('rows').value;
  const response = await fetch('/api/table-analysis', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ rows_count: parseInt(rows) })
  });
  
  const data = await response.json();
  
  document.getElementById('results').style.display = 'block';
  document.getElementById('giga-text').textContent = data.giga_result || 'Ошибка';
  document.getElementById('proxy-text').textContent = data.proxy_result || 'Ошибка';
}
</script>
```

---

## ⚠️ Частые проблемы

### ❌ "No data loaded"
```
Решение: Сначала загрузите файл через /api/upload
```

### ❌ "GigaChat API not initialized"
```
Решение: Проверьте GIGACHAT_TOKEN в .env файле
```

### ❌ "Request timeout"
```
Решение: API перегружена, повторите запрос через 10 сек
```

### ❌ Результаты пусты
```
Решение: Смотрите логи в logs/app.log для деталей
```

---

## 📁 Структура проекта (что где)

```
DataAnalitics_AI-Flask/
├── app/
│   ├── services/
│   │   └── analysis_service.py        ← analyze_table_first_rows()
│   ├── main.py                         ← /api/table-analysis
│   └── api/
│       ├── giga_chat.py               ← GigaChat API
│       └── proxy_api.py               ← Proxy API
├── test_table_analysis.py              ← Unit тесты
├── test_table_analysis_integration.py  ← Integration тесты
├── test_table_analysis_api.ps1         ← API примеры
├── TABLE_ANALYSIS_GUIDE.md             ← Полное руководство
└── CHANGES_SUMMARY.md                  ← Что изменилось
```

---

## 📊 Пример результата

### Загруженная таблица (первые 3 строки):
```
ID | Name     | Price | Quantity | Category
---|----------|-------|----------|----------
1  | Product1 | 100   | 10       | A
2  | Product2 | 150   | 15       | B
3  | Product3 | 200   | 20       | A
```

### Результат GigaChat:
```
Анализ данных показывает четкую тенденцию:
- Прямая корреляция между ценой и количеством товара
- Цены варьируются от 100 до 300 единиц с шагом ~50
- Распределение по категориям: 60% категория A, 40% категория B
- Все товары в наличии (количество > 0)
```

### Результат Proxy API:
```
Ключевые наблюдения:
- Таблица содержит хорошо структурированные данные о товарах
- Линейный тренд роста цены с увеличением количества
- Категории распределены равномерно
- Средняя цена: 150, стандартное отклонение: 50
```

---

## 🧪 Быстрый тест

### Используя curl
```bash
# Анализировать с параметрами по умолчанию
curl -X POST http://localhost:3000/api/table-analysis \
  -H "Content-Type: application/json" \
  -d "{}"

# Анализировать 10 строк
curl -X POST http://localhost:3000/api/table-analysis \
  -H "Content-Type: application/json" \
  -d "{\"rows_count\": 10}"
```

### Используя PowerShell
```powershell
# Загружаем функцию
.\test_table_analysis_api.ps1

# Или вручную
$body = @{ rows_count = 15 } | ConvertTo-Json
Invoke-RestMethod -Method POST `
  -Uri "http://localhost:3000/api/table-analysis" `
  -Headers @{ "Content-Type" = "application/json" } `
  -Body $body
```

---

## 🎓 Углубленное изучение

Для более подробной информации смотрите:

- **[TABLE_ANALYSIS_GUIDE.md](TABLE_ANALYSIS_GUIDE.md)** - полное руководство (450+ строк)
- **[CHANGES_SUMMARY.md](CHANGES_SUMMARY.md)** - что было добавлено и изменено
- **[test_table_analysis_api.ps1](test_table_analysis_api.ps1)** - примеры для тестирования
- **[app/services/analysis_service.py](app/services/analysis_service.py)** - исходный код сервиса

---

## 🔄 Рабочий процесс

```
Пользователь                 Frontend              Backend
    │                          │                      │
    ├─ Загружает файл ────────>│                      │
    │                          │─ POST /api/upload ──>│
    │                          │                      ├─ Парсинг
    │                          │<─ Успех ─────────────┤
    │                          │                      │
    ├─ Клик "Анализировать" ──>│                      │
    │                          │─ POST /table-analysis│
    │                          │                      ├─ analyze_table_first_rows()
    │                          │                      ├─ GigaChat
    │                          │                      ├─ Proxy API
    │<─ Результаты ────────────│<─ JSON ─────────────┤
    │                          │                      │
```

---

## ✅ Проверка готовности

Убедитесь, что все готово:

- [ ] Python установлен
- [ ] Виртуальное окружение активировано
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] `.env` файл настроен (GIGACHAT_TOKEN, PROXY_API_KEY)
- [ ] Приложение запущено (`python run.py`)
- [ ] Браузер открыт на `http://localhost:3000`

---

## 🎉 Готово!

Теперь вы можете:
1. ✅ Загружать файлы (CSV, Excel, PDF)
2. ✅ Анализировать первые N строк через AI
3. ✅ Получать результаты от двух нейросетей
4. ✅ Просматривать полные данные таблицы
5. ✅ Экспортировать результаты

**Приятного использования! 🚀**

---

**Версия:** 1.0  
**Дата:** 27 ноября 2025  
**Статус:** ✅ Production Ready
